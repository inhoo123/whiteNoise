<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.inhoo.white_noise.persistance.UserDao">

	<insert id="insert">
		INSERT INTO tbl_users(
			user_seq, user_id, user_password, user_nick,user_role
		) VALUES (
			#{user_seq},#{user_id},#{user_password},#{user_nick},#{user_role}
		)
	</insert>
</mapper> 